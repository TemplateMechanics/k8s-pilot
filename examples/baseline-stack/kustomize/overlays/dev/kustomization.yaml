apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

commonLabels:
  tier: dev

patches:
  - target: { kind: Deployment, name: baseline-web }
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
